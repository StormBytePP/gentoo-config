CFLAGS="${COMPILER_OPTIMIZATION_BASE} ${COMPILER_OPTIMIZATION_CPU} ${COMPILER_OPTIMIZATION_GCC}"
CXXFLAGS="${CFLAGS}"
LDFLAGS="${LINKER_OPTIMIZATION_BASE} ${LINKER_OPTIMIZATION_STRIP} ${LINKER_OPTIMIZATION_GOLD}"
CC="gcc"
CXX="g++"
AR="ar"
NM="nm"
RANLIB="ranlib"
